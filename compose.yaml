 services:
  locus-fulguris:
      build: .
      container_name: locus-fulguris
      restart: unless-stopped
      networks:
      - default
      - private_net
      env_file: ".env"
networks:
  private_net:
    external: true